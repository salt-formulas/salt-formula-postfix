{%- set address =  grains.ipv4[0] %}
check:
  remote_imap_server:
    command: "PATH=$PATH:/etc/sensu/plugins check-smtp.rb -h {{ address }} --tls"
    interval: 60
    occurrences: 1
    subscribers:
    - remote-network