applications:
  - postfix
parameters:
  postfix:
    server:
      enabled: false
      myorigin: ${linux:system:name}.${linux:system:domain}
    relay:
      enabled: true
      domain: ${linux:system:domain}
      interfaces: loopback-only
